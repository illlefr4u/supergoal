{
  "locked": false,
  "task": "{{TASK_TITLE}}",
  "created_at": "{{ISO_LOCKED}}",
  "max_turns": {{MAX_TURNS}},
  "plan_sha256": "{{HASH_PLAN}}",
  "acceptance_sha256": "{{HASH_ACCEPTANCE}}",
  "verify_sha256": "{{HASH_VERIFY}}",
  "polish_sha256": "{{HASH_POLISH}}",
  "required_phases": [
    "{{PHASE_1_NAME}}",
    "{{PHASE_2_NAME}}"
  ],
  "mandatory_commands": [
    "{{TYPECHECK_CMD}}",
    "{{LINT_CMD}}",
    "{{TEST_CMD}}",
    "{{BUILD_CMD}}"
  ],
  "conditional_triggers": [
    {"trigger": "schema_changed", "command": "{{MIGRATION_VALIDATE_CMD}}"},
    {"trigger": "ui_changed", "command": "{{UI_VERIFY_CMD}}"}
  ],
  "stop_conditions": [
    "all phases complete with evidence",
    "all mandatory verification passes or pre-existing failures proven unrelated",
    "polish passes complete",
    "FAILURE_PROBE completed",
    "SELF_REVIEW finds no blocking issues"
  ]
}
